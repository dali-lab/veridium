using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Veridium_Animation{
    public class AnimationManager : MonoBehaviour
    {/*
        [SerializeReference] public List<AnimGUI> references;

        public void RegisterReference(AnimGUI reference){
            if(references == null) references = new List<AnimGUI>();
            if(!references.Contains(reference)) {
                references.Add(reference);
                Debug.Log("Registered reference: " + reference.ToString());
            }
        }*/
    }
}
